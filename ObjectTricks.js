let obj = {
    name :"Mizbah",
    roll : 72,
    id   : 20701072,

    /*shorthand :::: if key_name == value_name
     x:x => x
     */
    
};


let keys = Object.keys(obj);
let values = Object.values(obj);
let entries = Object.entries(obj);

console.log(values);